#include "main.h"

/**
* _abs - change the number in absolute number
* @n: the number
* Return: return the absolute number
*/
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * (-1));
}

