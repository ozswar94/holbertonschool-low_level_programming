#include "main.h"
#include <stdio.h>
/**
* set_string - print a chess board
* @s:  pointer of pointer char
* @to: pointer char
* Return: nothing
*/
void set_string(char **s, char *to)
{
	char *tmp = (char *)*s;
	to = tmp;
	to++;
}
